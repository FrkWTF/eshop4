<%= link_to item.liquor.name, :action => 'show', :controller => 'catalog', :id => item.liquor.id %>

<%= pluralize item.amount, "licor", "licores" %>, <%= sprintf "%0.2f €", item.price * item.amount %>

(<%= link_to '<b>-</b>'.html_safe, :controller => 'cart', :action => 'remove', :id => item.liquor %>)
